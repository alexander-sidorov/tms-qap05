def test_example() -> None:
    assert True, "not True"
    a = 2
    b = 3
    result = a if a > b else b
    assert result
    assert a + b
    assert 100 / 2
    assert "South" + "Korea"
    assert "Busan" == "Busan"
    assert a != 3
    assert b - a = 1
